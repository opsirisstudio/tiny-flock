# Blender pipeline

Source `.blend` files for modular sheep and farm environment assets will live under `sheep/` and `environment/`. Exported assets should use Godot-compatible glTF 2.0 conventions. No models are part of the genetics milestone.
